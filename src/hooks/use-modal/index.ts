/**
 * useModal：弹窗一站式 hook（vben useVbenModal 风格），
 * 双端连接模式 + NModal 样板收敛，对外只暴露 useModal 入口与类型定义。
 */
export { useModal } from "./use-modal";

export type { ModalApi, UseModalOptions } from "./types";
